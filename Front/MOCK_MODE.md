# Mock 모드 - 백엔드 없이 프론트엔드 개발하기

백엔드 서버 없이 프론트엔드만 개발할 수 있는 Mock 데이터 모드입니다.

## 🎯 사용 방법

### 방법 1: 환경 변수 설정 (권장)

1. `Front` 폴더에 `.env.local` 파일 생성:

```bash
cd Front
touch .env.local
```

2. `.env.local` 파일에 다음 내용 추가:

```env
# Mock 데이터 모드 활성화
EXPO_PUBLIC_USE_MOCK_DATA=true

# API 설정 (Mock 모드에서는 사용되지 않음)
EXPO_PUBLIC_API_HOST=192.168.0.78
EXPO_PUBLIC_API_PORT=8083
```

3. 앱 재시작:

```bash
npm start
# 그리고 r 키를 눌러 reload
```

### 방법 2: app.json 설정

`Front/app.json`의 `extra` 섹션에 추가:

```json
{
  "expo": {
    "extra": {
      "useMockData": true
    }
  }
}
```

## ✅ Mock 모드에서 작동하는 기능

Mock 모드가 활성화되면:

1. **인증 시스템** ✨
   - **일반 로그인**: 아무 아이디/비밀번호로 로그인 가능
   - **회원가입**: 백엔드 없이 회원가입 가능
   - **테스트 계정**: 테스트 계정 버튼 작동
   - **asdf 로그인**: 개발용 빠른 로그인 작동
   - 자동으로 Mock 토큰 생성
   - 사용자 ID: 999
   - 사용자 이름: 입력한 이름 또는 "Mock User"

2. **데이터 표시**
   - 기존 하드코딩된 Mock 데이터 사용
   - `PRODUCT_DATA`, `ORDER_DATA` 등 활용

3. **UI 개발**
   - 모든 화면 정상 작동
   - 백엔드 연결 불필요
   - 빠른 UI 프로토타이핑 가능
   - 로그인/회원가입 플로우 완벽 작동

## 🔄 실제 백엔드로 전환

Mock 모드를 비활성화하려면:

1. `.env.local`에서 설정 변경:

```env
EXPO_PUBLIC_USE_MOCK_DATA=false
```

2. 또는 `.env.local` 파일 삭제

3. 앱 재시작

## 🛠️ 개발 팁

### Mock 모드 확인

코드에서 현재 모드 확인:

```typescript
import { USE_MOCK_DATA } from '../config/api';
import { isMockMode } from '../utils/DevTools';

// 방법 1
if (USE_MOCK_DATA) {
  // Mock 데이터 사용
}

// 방법 2
if (isMockMode()) {
  // Mock 데이터 사용
}
```

### Mock 인증 수동 설정

```typescript
import { setupMockAuth } from '../utils/DevTools';

// 앱 시작 시 Mock 인증 설정
await setupMockAuth();
```

## 📝 주의사항

1. **배포 시 비활성화**: 프로덕션 빌드 전에 Mock 모드를 반드시 비활성화하세요.

2. **API 호출 처리**: Mock 모드에서도 일부 API 호출이 실패할 수 있으므로, 필요한 경우 Service 파일에서 try-catch 처리를 추가하세요.

3. **데이터 동기화**: Mock 데이터는 실제 백엔드 데이터와 동기화되지 않으므로, 최종 테스트는 반드시 실제 백엔드와 함께 진행하세요.

## 🎨 활용 예시

### UI 디자인 작업
```bash
# Mock 모드로 전환
echo "EXPO_PUBLIC_USE_MOCK_DATA=true" > Front/.env.local

# 프론트 시작
cd Front
npm start
```

### 백엔드 개발 시 프론트 테스트
```bash
# Mock 모드 비활성화
rm Front/.env.local

# 백엔드 시작 후 프론트 시작
./start-dev.sh
```

## 🔧 문제 해결

### Mock 모드가 활성화되지 않는 경우

1. `.env.local` 파일 위치 확인 (`Front/.env.local`)
2. 앱 완전히 재시작 (Metro bundler 종료 후 재시작)
3. 캐시 삭제: `npx expo start --clear`

### Mock 인증이 작동하지 않는 경우

```typescript
// AsyncStorage 초기화 후 재설정
import AsyncStorage from '@react-native-async-storage/async-storage';
import { setupMockAuth } from './app/utils/DevTools';

await AsyncStorage.clear();
await setupMockAuth();
```

